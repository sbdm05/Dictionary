createDataSource(value_of_likes) { const ds = new ListView.DataSource({ rowHasChanged: (r1, r2) => r1 !== r2 }); this.dataSource = ds.cloneWithRows(value_of_likes); } render() { return ( <ListView enableEmptySections dataSource={this.dataSource} renderRow={this.renderRow} /> ); } likes_words.on('value', snapshot => { value_of_likes = snapshot.val() this.setState({ value_of_likes }); });
